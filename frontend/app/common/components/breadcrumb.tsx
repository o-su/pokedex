"use client";
import { Breadcrumb as CarbonBreadcrumb, BreadcrumbProps } from "@carbon/react";

export function Breadcrumb(props: BreadcrumbProps): JSX.Element {
  return <CarbonBreadcrumb {...props} />;
}
